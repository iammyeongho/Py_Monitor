# =============================================================================
# PyMonitor Docker 환경 변수
# =============================================================================
# Docker Compose 실행 전 이 파일을 .env로 복사하세요:
#   cp .env.docker .env
#
# docker-compose.yml에서 자동으로 .env 파일을 로드합니다.
# 프로덕션 환경에서는 SECRET_KEY와 POSTGRES_PASSWORD를 반드시 변경하세요.
# =============================================================================

# Database (Docker 내부 서비스명 사용)
# docker-compose.yml의 postgres 서비스로 연결
POSTGRES_SERVER=postgres
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
POSTGRES_DB=py_monitor
POSTGRES_PORT=5432

# Redis
REDIS_URL=redis://redis:6379/0
REDIS_ENABLED=false

# CORS 설정
# 쉼표로 구분하여 여러 도메인 허용 가능
# 예: http://192.168.0.10:8000,http://example.com
# * 사용 시 모든 도메인 허용 (개발 환경용)
BACKEND_CORS_ORIGINS=*

# Security (프로덕션에서는 반드시 강력한 랜덤 키로 변경)
SECRET_KEY=change-this-to-a-strong-random-key-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=11520

# Email (필요 시 실제 SMTP 서버 정보로 변경)
SMTP_HOST=dummy
SMTP_PORT=587
SMTP_USER=dummy
SMTP_PASSWORD=dummy
SMTP_TLS=True
SMTP_USERNAME=Py Monitor
SMTP_FROM=dummy@dummy.com

# Monitoring
DEFAULT_CHECK_INTERVAL=300
DEFAULT_TIMEOUT=30

# Logging
LOG_LEVEL=INFO
LOG_DIR=logs
